# netlify.toml – v4

[build]
  publish = "."
  functions = "netlify/functions"

# Route all API calls through the serverless function
[[redirects]]
  from = "/.netlify/functions/*"
  to   = "/.netlify/functions/:splat"
  status = 200

# Serve index-v4.html as the default page
[[redirects]]
  from = "/"
  to   = "/index-v4.html"
  status = 200
